.question
  = render @question
.answers
  = render 'answers'

= form_for [@question, @answer], remote: true do |f|
  .answer_errors
  =f.label :body, 'Your answer'
  =f.text_area :body
  =f.submit 'Create answer'
