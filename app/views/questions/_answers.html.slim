- @question.answers.each do |answer|
  - if answer.user == current_user
    p= answer.body
    = link_to 'Delete answer', question_answer_path(@question, answer.id), method: :delete if((answer.user.id == current_user.id) && signed_in?)